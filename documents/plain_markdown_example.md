# Plain Markdown Example

This is a normal Markdown file with no frontmatter.

The system will still index it correctly.

Default behavior:

- `title` comes from the filename
- `category` comes from the parent folder name
- `source_type` defaults to `file`
- no tags are attached unless you add them in frontmatter
